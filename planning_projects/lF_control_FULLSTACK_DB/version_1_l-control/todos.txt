1) компонент: TabGroups, text: 'вызывается два раза! Не использовать мощные вычисления здесь, а если да, то нужен fix!'

TASKS:

+ каким-то образом реализовать добавление файлов (изображений) на сервер
    - сделать чтобы изображения добавлялись по views отдельно и по всем views внутри основной таблицы
    (fileStructure: main_table -> view1, view2, view3 -> внутри каждого view дни)
            - а также чтобы эти изображения появлялись в зависимости от view таблицы,
            а в основной таблице - полностью (разделяясь по разделам)

            - изображения можно удалять, смотреть в полном размере (изначально они отображаются маленькими)

- сделать несколько лже-пользователей с паролями и логинами, чтобы в зависимости от пользователя
    - показывались данные только для него
    - в БД публиковались данные с его login в колонке user (например)
    - если пользователь Администратор, чтобы он видел данные по всем пользователям

- сделать простой интерфейс для добавления tabs, groups в зависимости от fields таблицы - далее компиляция на стороне web - сохранение в text_json

- сделать несколько input type в зависимости от field-type

- также сделать select - который берет значения из базы или из web (на подобии СИОПР select)

- сделать меню (на подобии СИОПР select)

- сделать редактируемые поля таблицы (при заданной настройке - чтобы не все поля таблицы могли быть редактируемыми)
- сделать сортировку полей (при заданной настройке - чтобы только определенные поля таблицы - или все - могли быть сортированы)
- сделать редактирование fields - visible, required (из базы и из интерфейса)

- сделать логику фильтрации данных таблицы (на подобии СИОПР)

####################################################################################

- придумать еще несколько логичных таблиц и их view, накатить туда данных

- пагинация
- фильтрация относительно пагинации

- оптимизация классов по БЭМ
- анимации для модального окна, пагинация с анимацией (preloader), анимация для перехода по tabs и переходов в меню

ВСЕ.
