Модели называем в единственном числе - а потом просто с помощью схемы

    public static function tableName()
    {
        return 'categories';
    }

возвращаем реальное име таблички из которой берется модель

// ленивая модель
// - если у нас прописана связь между двумя моделями
// - то она возникает только при использовании этой связи, то есть в момент обращения к связанной модели
// *** отложенную загрузку используйте крайне аккуратно - проверяйте сколько запросов выполняется - потому что одно
// дело когда мы по объекту из findOne делаем lazyLoading, другое дело когда по циклу из объектов в массиве.